apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: sureacccount-api
  labels:
    app.kubernetes.io/name: sureacccount-api
spec:
  hosts:
    - sureacccount-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: sureacccount-api
            port:
              number: 80
          weight: 100
